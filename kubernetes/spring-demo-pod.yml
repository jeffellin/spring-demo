apiVersion: v1
kind: Pod
metadata:
  name: spring-demo
  labels:
    version: "{VERSION}"
    app: spring-demo
spec:
  containers:
    - image: ellinj/spring-demo
      name: spring-demo
      ports:
        - containerPort: 8080
          name: http
          protocol: TCP
